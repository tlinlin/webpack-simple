const baseUrl = "http://localhost:9080"
const URL = {
        test: "/app/test",
        home: "/app/myhome",
        back: "/app/back",
    }
    // const keys = Object.keys(URL);
    // const lens = keys.length;
    // for (let i = 0; i < lens; i++) {
    //     URL[keys[i]] = baseUrl + URL[keys[i]];
    // }
export default URL;